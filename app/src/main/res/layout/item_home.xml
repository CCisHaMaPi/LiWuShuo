<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="10dp">

    <com.github.siyamed.shapeimageview.RoundedImageView
        android:id="@+id/item_home_image_view"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        app:siRadius="10dp"
        app:siBorderWidth="1dp"
        app:siBorderColor="@color/white"
        android:src="@drawable/ic_tab_profile_normal"
        />
    <ImageView
        android:id="@+id/item_home_new"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ig_marker_fresh"/>
    <CheckBox
        android:id="@+id/item_home_text_favorite"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        android:minWidth="30dp"
        android:maxWidth="50dp"
        android:textSize="12sp"
        android:focusable="false"
        android:clickable="true"
        android:gravity="center"
        android:scaleType="centerInside"
        android:textColor="@color/white"
        android:background="#50000000"
        android:button="@null"
        android:drawableTop="@drawable/home_list_item_favorite_selector"
        android:layout_alignParentRight="true"
        android:layout_marginRight="20dp"
        />
    <TextView
        android:id="@+id/item_home_text_title"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:background="@drawable/gradient_background"
        android:textSize="15sp"
        android:textColor="@color/white"
        android:text="这是一个标题"
        android:gravity="bottom"
        android:padding="15dp"
        android:layout_alignParentBottom="true"/>
</RelativeLayout>